URL: https://github.com/yosefshatila00/DI-bootcamp/blob/main/week_1/day_1/exercises/exerc.py
suggestions for improvement:
- In Exercise 3, the code should include the actual evaluation of the comparison operators and print the results instead of just writing the comments with expected results.  The comments are incorrect regarding type comparison. It should clearly show the boolean results (True/False) for each comparison.
- In Exercise 6 and 7, consider adding input validation to handle non-numeric input gracefully. For example, use a `try-except` block to catch `ValueError` if the user enters something that can't be converted to an integer.
- Improve the style and clarity of Exercise 8's output message. It currently feels a little abrupt. A more engaging message would enhance the user experience.
- Add comments to explain what each part of the code does. This will improve readability, especially for longer exercises.
Brief justification:
- correctness: The code correctly addresses most of the exercises outlined in the chapter content.  It demonstrates the use of print statements, variables, mathematical operations, comparison operators, if statements, and user input. The exceptions are in Exercise 3 where the code only shows the comments (and the comments are partly incorrect)  instead of evaluating the expressions and printing boolean results and the minor style and feedback issues mentioned in the feedback.
- readability: The code is mostly readable, but adding comments and better formatting (especially in Exercise 3 where it's just comments without code and the slightly abrupt responses in Exercise 8) would significantly improve its understandability. The lack of input validation in Exercises 6 and 7 slightly detracts from readability and robustness.
- performance: The code's performance is acceptable for these simple exercises.  No significant performance improvements are needed.
- security: The code does not present any security vulnerabilities at this level of complexity. User input is handled, but error handling could be improved as mentioned in the feedback for better robustness.

